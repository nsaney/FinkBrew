In this directory ("BrewInterface", which contains the directory "chairosoft"), run the following command:
$> python -m chairosoft.brew_interface

Note that the following modules are required (project name given in parentheses):
- serial (pySerial)
- enum (python-enum)